"use strict";

$(document).ready(function () {
  $('.main_wrapper').slick({
    setting_name: setting - value
  });
});
$('.single-item').slick({
  dots: true,
  arrows: false,
  focusOnSelect: true
});